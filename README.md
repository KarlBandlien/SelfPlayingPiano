# SelfPlayingPiano
In blender I made a model of a piano and marbles, then I used a script to transform any midi file into a CSV that I could insert into another script in blender that would make keyframes in every position a note would be hit and it would use more marbles depending on if the marbles are used.
